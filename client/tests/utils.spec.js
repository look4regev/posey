var assert = require("assert");

describe("Array", function() {
  describe("#toTuple()", function() {
    it("placeholder until we'll have tests", function() {
      assert.equal(1, 1);
    });
  });
});
